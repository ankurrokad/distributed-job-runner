{
  "name": "@repo/worker",
  "version": "0.1.0",
  "private": true,
  "description": "BullMQ worker process with job handlers",
  "scripts": {
    "build": "tsc",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "lint": "eslint \"src/**/*.ts\"",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "bullmq": "^5.1.0",
    "@repo/db": "workspace:*",
    "@repo/shared": "workspace:*",
    "@repo/queue": "workspace:*",
    "ioredis": "^5.3.2"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3"
  }
}

